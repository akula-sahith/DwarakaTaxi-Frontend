import HeroLayout from "../layout/HeroLayout";
import HeroMain from "./HeroMain";

const HeroSection = () => {
  return (
    <HeroLayout>
      <HeroMain />
    </HeroLayout>
  );
};

export default HeroSection;
